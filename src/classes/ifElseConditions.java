package classes;

public class ifElseConditions {
    static void main() {
        String a="hello";
        String b="Hello";
        System.out.println(a.equals(b));//case sensitive
        System.out.println(a.equalsIgnoreCase(b));// only compare value case doesn't matters;

    }

}
